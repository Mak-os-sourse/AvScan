services:
  db:
    image: postgres:alpine

    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 12345678
      POSTGRES_DB: test
    ports:
      - "5434:5432"
    tmpfs:
      - /var/lib/postgresql/db
  redis:
    image: redis:alpine

    ports:
      - "6378:6379"